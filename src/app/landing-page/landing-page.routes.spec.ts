import { LandingPageRoutes } from './landing-page.routes';

describe('LandingPageRoutes', () => {
  it('should create an instance', () => {
    expect(new LandingPageRoutes()).toBeTruthy();
  });
});
